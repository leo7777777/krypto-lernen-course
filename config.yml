title: Krypto lernen
description: Kryptowährungen beschreiben
template:
  repo: krypto-lernen-template
  name: krypto-lernen
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
